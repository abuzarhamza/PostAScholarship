#!/usr/bin/perl

use warnings;
use strict;

our %CONFIG = ();

$CONFIG{'SQL_DIRPATH'}      = '../../mysql/';
$CONFIG{'SQL_CRTABLE_FILE'} = 'createTable.sql';
$CONFIG{'SQL_CRDB_FILE'}    = 'createDB.sql';